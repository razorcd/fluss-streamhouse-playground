# This is the configuration for Log4j 2

# Set the default logging level to DEBUG to see everything
status = info
name = FlinkLog4j2PropertiesConfig

# Console appender configuration
appender.console.type = Console
appender.console.name = ConsoleAppender
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{yyyy-MM-dd HH:mm:ss,SSS} %-5p %-60c %x - %m%n

# Root logger, applies to everything unless overridden
rootLogger.level = ERROR   
rootLogger.appenderRef.console.ref = ConsoleAppender

# --- Checkpoint Logging Configuration ---
# You can keep these specific loggers, but the rootLogger.level = DEBUG will already show them.
# They are useful if you later change the rootLogger level back to INFO.

# # Logger for the main CheckpointCoordinator (on the JobManager)
# logger.checkpoint_coordinator.name = org.apache.flink.runtime.checkpoint.CheckpointCoordinator
# logger.checkpoint_coordinator.level = INFO

# # Logger for checkpoint barrier handling (on TaskManagers)
# logger.checkpoint_barrier_handler.name = org.apache.flink.runtime.io.network.partition.consumer.CheckpointBarrierHandler
# logger.checkpoint_barrier_handler.level = INFO

# # Logger for the state backend interactions
# logger.state_backend.name = org.apache.flink.runtime.state
# logger.state_backend.level = INFO

# # Logger for the completion of checkpoints
# logger.checkpoint_completion.name = org.apache.flink.runtime.checkpoint.CompletedCheckpointStore
# logger.checkpoint_completion.level = INFO